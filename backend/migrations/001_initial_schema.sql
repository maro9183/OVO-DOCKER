-- Baseline migration (Empty or comment since schema.sql handles initial creation)
-- This file exists to establish the versioning baseline.
SELECT 'Base schema initialized via schema.sql' AS status;
